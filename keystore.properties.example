# Copy to keystore.properties only after scripts/create-upload-key.sh has generated your key.
# Do not commit keystore.properties or the referenced .jks file.
storeFile=.local-signing/api-flow-upload.jks
storePassword=REPLACE_WITH_A_SECRET
keyAlias=api-flow-upload
keyPassword=REPLACE_WITH_A_SECRET
